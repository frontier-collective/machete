{
  "identifier": "default",
  "description": "Default capabilities for the Machete app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "shell:allow-open"
  ]
}
